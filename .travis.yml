language: java
matrix:
  include:
    - jdk: oraclejdk8
script: travis_retry ./gradlew headless clean allTests coverage coveralls -i
before_install:
          - "export DISPLAY=:99.0"
          - "sh -e /etc/init.d/xvfb start"

addons:
  apt:
    packages:
      - oracle-java8-installer

